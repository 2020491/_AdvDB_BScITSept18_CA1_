tasks:
  name: Running MongoDB
  #- init: echo "Replace me with a build script for the project."
    command: npm i -g nodemon && mkdir -p data && mongod --dbpath data #mkdir -p /workspace/data && mongod --dbpath /workspace/data --fork --logpath /workspace/data/mongod.log
image:
  file: .gitpod.Dockerfile 
